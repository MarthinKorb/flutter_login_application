import 'package:flutter/material.dart';

class ThemeColor {
  static const Color primaryColor = Colors.green;
  static const Color secundaryColor = Colors.white;
  static const Color bottomAppBarColor = Colors.white;

  static const MaterialColor color = Colors.green;
  static const MaterialColor canvasColor = Colors.green;

  static const Color unselectedIcons = Colors.black54;
  static const Color headers = Colors.black54;

  static const MaterialColor buttonColor = Colors.green;
  static const MaterialColor cardColor = Colors.lightBlue;

  static const MaterialColor warningColor = Colors.amber;
  static const MaterialColor successColor = Colors.green;
  static const MaterialColor errorColor = Colors.red;

  static const MaterialColor containerColor = Colors.deepPurple;
}
